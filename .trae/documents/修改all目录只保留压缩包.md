## 目标
修改updater.py，使all目录只保留压缩包，不保留技能文件夹

## 实施步骤
1. 修改`create_individual_skill_zips`方法，在生成压缩包后删除对应的技能文件夹
2. 或者修改`sync_to_all`方法，不复制文件夹，直接生成压缩包到all目录
3. 确保all目录最终只包含`.zip`文件，不包含其他文件夹

## 方案选择
采用方案2：修改`sync_to_all`方法，直接读取原始技能目录，生成压缩包到all目录，不复制整个文件夹结构。这样all目录只会包含压缩包文件。